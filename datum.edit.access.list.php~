<?php
foreach($db->datum_get_accessors($id) as $accessor){
	if(count($accessors) == 0 ){
		echo "No accessors yet<br/>";
	}else{	
		print_r($accessor);
	}
}
foreach($db->datum_get_requestors($userid,$id) as $accessor){
        if(count($accessors) == 0 ){
                echo "No accessors yet<br/>";
        }else{  
		print_r($accessor);
	}   
}
?>
